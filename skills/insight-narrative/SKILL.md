# Insight narrative

After ingest, run P&L, Cash, and Growth in parallel. Each returns KPIs plus widget specs.

Explain variances in plain CFO language. Prefer `query_cube` over the sandbox. If the cube cannot answer, say so.
