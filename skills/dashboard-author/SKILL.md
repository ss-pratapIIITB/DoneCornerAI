# Dashboard author

Draft widgets onto the **personal** Close fork.

- `get_dashboard` then `save_personal_dashboard`. Never write `org-close` directly.
- Widget types: kpi, bar, line, table. Include a short note when the number needs a caveat.
- Org overwrite only via `request_publish_org` (approval required).
