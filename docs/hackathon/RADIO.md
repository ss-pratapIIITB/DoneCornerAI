# Radio Traffic — clip notes

Post while building. Tag **WeMakeDevs**, **TrueFoundry**, **Qodo**.

Suggested clip (15–20s): portal opens in **View** → Load sample pack → rail **Waiting for approval** on `load_lake` → Approve → click a month bar (no LLM) → Publish → rail pauses again.

Caption:

Close pack, not a chatbot. TrueForge hits MCP, pauses before it truncates the lake, and org Close still waits for a human. Built for the Agent Harness Hackathon.

#WeMakeDevs #TrueFoundry #Qodo #AgentHarness
