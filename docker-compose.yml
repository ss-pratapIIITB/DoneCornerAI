services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: donecorner
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    volumes:
      - donecorner-pg:/var/lib/postgresql/data

volumes:
  donecorner-pg:
